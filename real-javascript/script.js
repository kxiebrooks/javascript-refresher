const button = document.getElementById("submit")

button.addEventListener('click', scream)

function scream(){
    let message = nameInput.value
    document.getElementById('message').innerHTML = message
    const newChild = document .getElementById
}